name: Final Equalizer UI Build

# Keep the manual entry point, but build the tested source instead of
# overwriting EqualizerActivity with an obsolete embedded Kotlin template.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-build:
    uses: ./.github/workflows/equalizer-regression.yml
